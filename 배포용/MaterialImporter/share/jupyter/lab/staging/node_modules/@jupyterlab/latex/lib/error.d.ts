import { Message } from '@phosphor/messaging';
import { Widget } from '@phosphor/widgets';
import * as React from 'react';
/**
 * A widget which hosts the error logs from LaTeX
 * when document compilation fails.
 */
export declare class ErrorPanel extends Widget {
    /**
     * Construct the error panel.
     */
    constructor();
    text: string;
    /**
     * Handle an update request.
     */
    protected onUpdateRequest(msg: Message): void;
    /**
     * Handle a close request.
     */
    protected onCloseRequest(msg: Message): void;
}
export interface ILatexProps extends React.Props<LatexError> {
    text: string;
}
export declare class LatexError extends React.Component<ILatexProps, {}> {
    render(): JSX.Element;
}
